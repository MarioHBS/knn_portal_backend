# Arquivo de exemplo para configuração de variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Modo de teste - quando true, usa dados simulados sem conectar aos bancos reais
KNN_USE_TEST_DATABASE=false

# Modo de teste local - quando true, desabilita autenticação para facilitar desenvolvimento
TESTING_MODE=false

# Modo debug - quando true, ativa logs detalhados
DEBUG=false

# Ambiente de execução (development, staging, production)
ENVIRONMENT=development

# Porta do servidor
PORT=8080

# Configurações de banco de dados
FIRESTORE_PROJECT=knn-portal-dev
POSTGRES_CONNECTION_STRING=postgresql://postgres:postgres@localhost:5432/knn_portal

# Configurações de autenticação do Firestore
# Opção 1: Caminho para o arquivo de service account key (recomendado para desenvolvimento)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Opção 2: JSON da service account key como string (para produção com variáveis de ambiente)
# FIRESTORE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}

# Configurações de múltiplos bancos Firestore
FIRESTORE_DATABASES=["(default)","knn-benefits"]
FIRESTORE_DATABASE=0

# Configurações de segurança (ALTERE EM PRODUÇÃO)
CNPJ_HASH_SALT=knn-dev-salt
CPF_HASH_SALT=knn-dev-cpf-salt

# Configurações de autenticação
JWKS_URL=https://auth.knnidiomas.com.br/.well-known/jwks.json
JWKS_CACHE_TTL=600

# Configurações de circuit breaker
CIRCUIT_BREAKER_THRESHOLD=3
CIRCUIT_BREAKER_TIMEOUT=300

# Configurações de rate limit
RATE_LIMIT_REDEEM=5/minute

# Modo de operação (normal ou degraded)
MODE=normal