# Arquivo de exemplo para configuração de variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário.

# --- Ambiente de Execução ---
# Define o ambiente (development ou production).
# Em produção, o banco de dados do Firestore será 'knn-benefits' e o modo DEBUG será desativado.
# Em desenvolvimento, o banco de dados será '(default)' e o modo DEBUG será ativado.
ENVIRONMENT=development

# --- Configurações da API ---
PORT=8080

# --- Configurações de Banco de Dados ---
FIRESTORE_PROJECT=knn-benefits
POSTGRES_ENABLED=false
POSTGRES_CONNECTION_STRING=postgresql://user:password@host:port/database

# --- Configurações de Autenticação ---
# Em produção, a variável GOOGLE_APPLICATION_CREDENTIALS é gerenciada pelo Google Cloud.
# Para desenvolvimento local, descomente e aponte para o seu arquivo de credenciais.
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/service-account-key.json

JWKS_URL=https://auth.knnidiomas.com.br/.well-known/jwks.json
JWKS_CACHE_TTL=600
JWT_SECRET_KEY=sua-chave-secreta-jwt-aqui

# --- Configurações de Segurança ---
# É altamente recomendável alterar estes valores em produção.
CNPJ_HASH_SALT=seu-salt-para-cnpj
CPF_HASH_SALT=seu-salt-para-cpf

# --- Configurações do Firebase Storage ---
FIREBASE_STORAGE_BUCKET=seu-bucket-de-storage.appspot.com

# --- Configurações de Circuit Breaker ---
CIRCUIT_BREAKER_THRESHOLD=3
CIRCUIT_BREAKER_TIMEOUT=300

# --- Configurações de Rate Limit ---
RATE_LIMIT_REDEEM=5/minute

# --- Modo de Operação ---
# 'normal' usa o Firestore como primário.
# 'degraded' usa o PostgreSQL como primário.
MODE=normal